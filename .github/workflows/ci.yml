name: Smart Mirror CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x] # Ensure we test on Node 18 (what your Dockerfile uses)

    steps:
    # 1. Check out the code
    - name: Checkout repository
      uses: actions/checkout@v4

    # 2. Set up Node.js
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}

    # 3. Install backend dependencies (ci = clean install)
    - name: Install Backend Dependencies
      working-directory: ./backend
      run: npm ci

    # 4. Run the hardware abstraction Jest unit tests
    - name: Run Backend Unit Tests
      working-directory: ./backend
      run: npm test

    # 5. Verify the frontend React code compiles without fatal errors
    - name: Build Mobile PWA
      working-directory: ./mobile-pwa
      run: |
        npm ci
        npm run build

    # 6. Verify the mirror display bundle compiles without fatal errors
    - name: Build Mirror Display
      working-directory: ./display
      run: |
        npm ci
        npm run build

    # 7. Keep dependency audit visible, but do not block the build-and-test job
    # until the current package audit backlog is intentionally remediated.
    - name: Audit Backend Dependencies
      working-directory: ./backend
      continue-on-error: true
      run: npm audit
